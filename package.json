{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build || exit 0",
    "preview": "astro preview",
    "test": "echo \"no test specified\" && exit 0"
  },
  "devDependencies": {
    "astro": "^0.20.0"
  }
}