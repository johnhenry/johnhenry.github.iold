import { html } from "lit-html";
import { unsafeHTML } from "lit-html/directives/unsafe-html.js";
import { Meta, Canvas } from "@storybook/addon-docs/blocks";
const HOST = "https://johnhenry.github.io/";
const Template = ({ name = "butter" }) => html` <style>
    @import "${HOST}std/css/colors-tango@0.0.0/index.css";
    .${name} {
      display: flex;
    }
    .${name} div {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100px;
      height: 100px;
      background-color: var(--color-tango-${name});
    }
    .${name} div::before {
      width: 100%;
      text-align: center;
      height: fit-content;
      content: var(--text-tango-${name});
      color: var(--color-tango-${name}-complement);
      content-editable: true;
    }
    .${name} .highlight {
      background-color: var(--color-tango-${name}-highlight);
    }
    .${name} .highlight::before {
      content: var(--text-tango-${name}-highlight);
      color: var(--color-tango-${name}-highlight-complement);
    }
    .${name} .shadow {
      background-color: var(--color-tango-${name}-shadow);
    }
    .${name} .shadow::before {
      content: var(--text-tango-${name}-shadow);
      color: var(--color-tango-${name}-shadow-complement);
    }
  </style>
  <div class="${name}">
    <div class="highlight"></div>
    <div></div>
    <div class="shadow"></div>
  </div>`;

<Meta
  title="CSS Library/Tango Colors/Docs"
  argTypes={{
    name: {
      options: [
        "butter",
        "orange",
        "chocolate",
        "chameleon",
        "sky",
        "plum",
        "scarlet",
        "aluminium",
        "slate",
      ],
      control: {
        type: "select",
      },
    },
  }}
/>

# Tango Colors

<Canvas>
  <Story name="Colors" args={{ name: "butter" }}>
    {Template.bind({})}
  </Story>
</Canvas>
